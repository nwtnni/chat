(executable
  (name client)
  (public_name client)
  (modules client)
  (libraries chat lwt lwt.unix ANSITerminal))

(executable
  (name server)
  (public_name server)
  (modules server)
  (libraries chat lwt lwt.unix ANSITerminal str))
